# Action module

The readme here is for the `action` module of the `cracha` app.
The action module will be responsible to manage everything regarding the action, but let's clarify what is an action:
An action is something that the user can do, like create/read/delete/update, etc.
 
